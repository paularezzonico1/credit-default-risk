"""Credit-default-risk scoring API package."""

__all__ = ["__version__"]

# Service/API version (independent of the ML model version, which is derived
# from the serialized artifact at runtime — see api.model_service).
__version__ = "2.0.0"
